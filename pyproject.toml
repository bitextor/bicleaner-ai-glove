[tool.cibuildwheel]
build = "cp3{9,10,11,12,13}-manylinux_x86_64"
manylinux-x86_64-image = "manylinux2014"

[build-system]
requires = ["Cython==3.2.5", "setuptools>=48"]
build-backend = "setuptools.build_meta"
